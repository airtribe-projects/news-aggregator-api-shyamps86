const {NewsRepositary}  = require("../repositaries/index.js");
const ApiError = require("../utils/ApiError.js");

const NewsRespositary = new NewsRepositary();

const getAllNews = () => {

};

const createNews = (data) => {
    
};

const updateNews = (id, data) => {
    
};

const deleteNews = (id) => {
   
};

const getNews = (id) => {
   
};

const patchNews = (id, data) => {
   
};


module.exports = { createNews, deleteNews, updateNews, getAllNews, patchNews, getNews };
